{
  "name": "@shared/auth",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "auth:generate": "npx @better-auth/cli generate --config ./src/cli-config.ts --output ../database/src/auth.initial.sqlite",
    "auth:migrate": "npx @better-auth/cli migrate --config ./src/cli-config.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@shared/database": "workspace:*",
    "better-auth": "catalog:"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.3.25",
    "@shared/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}